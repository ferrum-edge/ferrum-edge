name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-

      - name: Cache cargo index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-git-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-git-

      - name: Cache cargo build
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-build-target-

      - name: Install protoc
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler

      - name: Run tests
        run: cargo test --verbose --all-features
        env:
          RUST_BACKTRACE: 1

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          components: rustfmt, clippy

      - name: Install protoc
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo build
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}

      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Check formatting
        run: cargo fmt --all -- --check

  # TODO: Enable build-release and docker-build jobs when the project is mature
  # build-release:
  #   name: Build Release
  #   needs: [test, lint]
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   runs-on: ${{ matrix.os }}
  #   strategy:
  #     matrix:
  #       include:
  #         - os: ubuntu-latest
  #           target: x86_64-unknown-linux-gnu
  #           artifact_name: ferrum-gateway
  #           asset_name: ferrum-gateway-linux-x86_64
  #
  #         - os: macos-latest
  #           target: x86_64-apple-darwin
  #           artifact_name: ferrum-gateway
  #           asset_name: ferrum-gateway-macos-x86_64
  #
  #         - os: macos-latest
  #           target: aarch64-apple-darwin
  #           artifact_name: ferrum-gateway
  #           asset_name: ferrum-gateway-macos-aarch64
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Install Rust
  #       uses: dtolnay/rust-toolchain@stable
  #       with:
  #         toolchain: stable
  #         targets: ${{ matrix.target }}
  #
  #     - name: Install protoc (Ubuntu)
  #       if: runner.os == 'Linux'
  #       run: |
  #         sudo apt-get update
  #         sudo apt-get install -y protobuf-compiler
  #
  #     - name: Install protoc (macOS)
  #       if: runner.os == 'macOS'
  #       run: brew install protobuf
  #
  #     - name: Cache cargo registry
  #       uses: actions/cache@v4
  #       with:
  #         path: ~/.cargo/registry
  #         key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
  #
  #     - name: Cache cargo build
  #       uses: actions/cache@v4
  #       with:
  #         path: target
  #         key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}
  #
  #     - name: Build release binary
  #       run: cargo build --release --target ${{ matrix.target }}
  #
  #     - name: Create asset directory
  #       run: mkdir -p release-assets
  #
  #     - name: Copy binary to release assets
  #       if: runner.os != 'macOS'
  #       run: cp target/${{ matrix.target }}/release/${{ matrix.artifact_name }} release-assets/${{ matrix.asset_name }}
  #
  #     - name: Copy binary to release assets (macOS)
  #       if: runner.os == 'macOS'
  #       run: cp target/${{ matrix.target }}/release/${{ matrix.artifact_name }} release-assets/${{ matrix.asset_name }}
  #
  #     - name: Upload release artifacts
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: ${{ matrix.asset_name }}
  #         path: release-assets/${{ matrix.asset_name }}
  #
  # docker-build:
  #   name: Build and Push Docker Image
  #   needs: [test, lint]
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   env:
  #     HAS_DOCKER_CREDS: ${{ secrets.DOCKER_USERNAME != '' && secrets.DOCKER_PASSWORD != '' }}
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v2
  #
  #     - name: Log in to Docker Hub
  #       if: env.HAS_DOCKER_CREDS == 'true'
  #       uses: docker/login-action@v2
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}
  #
  #     - name: Build and push Docker image
  #       if: env.HAS_DOCKER_CREDS == 'true'
  #       uses: docker/build-push-action@v4
  #       with:
  #         context: .
  #         push: true
  #         tags: |
  #           ${{ secrets.DOCKER_USERNAME }}/ferrum-gateway:latest
  #           ${{ secrets.DOCKER_USERNAME }}/ferrum-gateway:${{ github.sha }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max
  #
  #     - name: Build Docker image (no push)
  #       if: env.HAS_DOCKER_CREDS != 'true'
  #       uses: docker/build-push-action@v4
  #       with:
  #         context: .
  #         push: false
  #         tags: ferrum-gateway:latest
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max
