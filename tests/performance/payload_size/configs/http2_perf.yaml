---
# HTTP/2 payload size test config
# Gateway listens on HTTPS (8443), proxies to HTTPS/H2 backend
proxies:
  - id: "h2-echo"
    name: "HTTP/2 Echo"
    listen_path: "/echo"
    backend_scheme: https
    backend_host: "127.0.0.1"
    backend_port: 4443
    strip_listen_path: false
    preserve_host_header: false
    backend_connect_timeout_ms: 5000
    backend_read_timeout_ms: 30000
    backend_write_timeout_ms: 30000
    backend_tls_verify_server_cert: false
    auth_mode: single
    pool_enable_http2: true
    pool_http2_keep_alive_interval_seconds: 30
    pool_http2_keep_alive_timeout_seconds: 45
    pool_http2_initial_stream_window_size: 8388608
    pool_http2_initial_connection_window_size: 33554432
    pool_http2_adaptive_window: true
    pool_http2_max_frame_size: 1048576
    pool_http2_max_concurrent_streams: 1000
    plugins: []

  - id: "h2-sse"
    name: "HTTP/2 SSE"
    listen_path: "/sse"
    backend_scheme: https
    backend_host: "127.0.0.1"
    backend_port: 4443
    strip_listen_path: false
    preserve_host_header: false
    backend_connect_timeout_ms: 5000
    backend_read_timeout_ms: 30000
    backend_write_timeout_ms: 30000
    backend_tls_verify_server_cert: false
    auth_mode: single
    pool_enable_http2: true
    pool_http2_initial_stream_window_size: 8388608
    pool_http2_initial_connection_window_size: 33554432
    pool_http2_adaptive_window: true
    pool_http2_max_frame_size: 1048576
    pool_http2_max_concurrent_streams: 1000
    plugins: []

  - id: "h2-health"
    name: "Health Check"
    listen_path: "/health"
    backend_scheme: http
    backend_host: "127.0.0.1"
    backend_port: 4010
    strip_listen_path: false
    preserve_host_header: false
    backend_connect_timeout_ms: 1000
    backend_read_timeout_ms: 5000
    backend_write_timeout_ms: 5000
    auth_mode: single
    plugins: []

consumers: []
plugin_configs: []
